for (let i = 0; i < 5;) {
  console.log(i += 1);
}

/*
this code does not have the increment step which is optional so no error will be produed

the increment step occurs on line 2 instead of in the same line as the initializer and stopping condition

*/
