console.log('5' + 10)

// implicit type conversion - JS converts the number 10 to a string which leads to string concatenation
// every + expression that has a string operand produces a string result
// number 10 gets coerced into a string then concatenated to String '5
